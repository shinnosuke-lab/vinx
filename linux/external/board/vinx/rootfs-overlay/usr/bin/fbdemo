#!/bin/sh
# fbdemo -- paint the brand gradient on the VGA screen. The painting lives
# in /usr/share/vinx/fbdemo.py; this wrapper is the fb-run doorway every
# framebuffer program passes (one at a time, stty restored, §10.5).
exec fb-run micropython /usr/share/vinx/fbdemo.py "$@"
